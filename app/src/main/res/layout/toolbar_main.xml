<?xml version="1.0" encoding="utf-8"?>
<!-- "?attr/actionBarSize" =  altura estándar de una barra de acción -->
<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="?attr/actionBarSize"
    android:background="@color/club_primary_blue">


    <androidx.appcompat.widget.Toolbar
        android:id="@+id/main_toolbar"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:contentInsetStart="0dp"
        android:contentInsetLeft="0dp"
        android:theme="@style/ThemeOverlay.AppCompat.Dark.ActionBar">
        <!-- android:theme="@style/ThemeOverlay.AppCompat.Dark.ActionBar" =  íconos y textos dentro de la Toolbar sean claros -->

        <!-- LOGO -->
        <ImageView
            android:layout_width="40dp"
            android:layout_height="40dp"
            android:layout_marginStart="8dp"
            android:contentDescription="Logo del club"
            android:src="@drawable/logo" />



        <!-- Texto -->
        <TextView
            android:id="@+id/toolbar_title"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="start"
            android:layout_marginStart="16dp"
            android:text="Club Deportivo Administración"
            android:textColor="@android:color/white"
            android:textSize="18sp" />

        <!-- MENU HAMBURGESA -->
        <ImageButton
            android:id="@+id/toolbar_menu_button"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="end"
            android:layout_marginEnd="16dp"
            android:background="?attr/selectableItemBackgroundBorderless"
            android:src="@drawable/menu_burger_24" />

    </androidx.appcompat.widget.Toolbar>

</LinearLayout>